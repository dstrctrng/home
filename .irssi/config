settings = {
  "fe-common/core" = {
    # Solarized
    theme = "solarized-universal";
    hilight_color = "= %R";
  };
  core = {
    real_name = "where's the fire?";
    user_name = "defn";
    nick = "defn";
  };
  "fe-text" = { actlist_sort = "refnum"; };
};

hilights = ( 
  { text = "defn"; color = "%M"; nick = "yes"; word = "yes"; }
);

statusbar = {
  items = {
    # Solarized
    lag = "{sb Lag: %m$0-%n}";
    act = "{sb Act: $0-}";
    more = "%k%3-- more --%n";
  };
};
