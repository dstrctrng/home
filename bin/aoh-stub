#!/bin/bash

#/ NAME
#/     aoh-stub -- stuff for anchoring alpha_omega deploy helpers to aoh-
#/

# figure out the project root under which bin, lib live
shome="$(cd -P -- "$(dirname -- "$BASH_SOURCE")/.." && pwd -P)"

"$shome/libexec/aoh" "${BASH_SOURCE##*/}" "$@"
